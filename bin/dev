#!/usr/bin/env bash
set -euo pipefail
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
if [[ $# -eq 0 ]]; then
  exec "$ROOT_DIR/script/build_and_run.sh"
fi
exec "$ROOT_DIR/script/build_and_run.sh" "$@"
